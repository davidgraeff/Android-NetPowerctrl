package oly.netpowerctrl.utils.fragments;

/**
 * Used to provide fragments with the ability to react to the back button of the main activity.
 */
public interface FragmentOnBackButton {
    boolean onBackButton();
}
